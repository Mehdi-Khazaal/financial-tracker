fastapi>=0.115.0
uvicorn>=0.32.0
alembic>=1.14.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
pydantic>=2.0.0
PyJWT==2.13.0
bcrypt>=4.0.0
# 50.0.0 is the floor that clears PYSEC-2026-3552/3553/3554. Pinned exactly
# because Render installs from wheels; bump deliberately, not by range.
cryptography==50.0.0
email-validator>=2.0.0
python-multipart>=0.0.20
requests>=2.31.0
yfinance>=0.2.40
slowapi>=0.1.9
pywebpush>=2.0.0
# 0.117 is the floor for adaptive thinking + output_config used by the assistant.
anthropic>=0.117.0
# zoneinfo has no bundled database on Windows; Render's Linux image does.
tzdata>=2026.1
